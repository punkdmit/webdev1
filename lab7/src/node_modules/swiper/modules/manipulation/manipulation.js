import appendSlide from 'swiper/modules/manipulation/methods/appendSlide.js';
import prependSlide from 'swiper/modules/manipulation/methods/prependSlide.js';
import addSlide from 'swiper/modules/manipulation/methods/addSlide.js';
import removeSlide from 'swiper/modules/manipulation/methods/removeSlide.js';
import removeAllSlides from 'swiper/modules/manipulation/methods/removeAllSlides.js';
export default function Manipulation({
  swiper
}) {
  Object.assign(swiper, {
    appendSlide: appendSlide.bind(swiper),
    prependSlide: prependSlide.bind(swiper),
    addSlide: addSlide.bind(swiper),
    removeSlide: removeSlide.bind(swiper),
    removeAllSlides: removeAllSlides.bind(swiper)
  });
}